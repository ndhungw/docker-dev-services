# DOCKER DEV SERVICES

## What inside?

- redis:alpine
- postgresql:alpine

## How to start

```(bash)
docker compose up -d
```

## How to change config

Read the [docker-compose.yml](./docker-compose.yml) and update the values in [.env](./.env)
